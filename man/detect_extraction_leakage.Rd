% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraction-leakage.R
\name{detect_extraction_leakage}
\alias{detect_extraction_leakage}
\title{Detect covariate-extraction-overlap leakage}
\usage{
detect_extraction_leakage(data, split, radius, coords = NULL)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{split}{A split specification (see \code{\link[=detect_leakage]{detect_leakage()}}).}

\item{radius}{The extraction-window radius / buffer / bandwidth, in coordinate
units (geodesic metres for geographic CRS).}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}
}
\value{
An object of class \code{extraction_leakage}.
}
\description{
Flags test points whose covariate-extraction window overlaps a training point's,
given the extraction radius (focal-window radius, buffer distance, or kernel
bandwidth) used to build the spatial covariates.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(100), y = runif(100), z = rnorm(100))
# covariates were extracted with a 0.1-unit focal window:
detect_extraction_leakage(d, split = sample(rep_len(1:5, 100)),
                          radius = 0.1, coords = c("x", "y"))
}
