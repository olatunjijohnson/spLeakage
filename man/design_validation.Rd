% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design-validation.R
\name{design_validation}
\alias{design_validation}
\title{Design an optimal independent validation sample}
\usage{
design_validation(
  data,
  target,
  budget,
  response = NULL,
  coords = NULL,
  candidates = NULL,
  allocation = c("optimal", "proportional", "random"),
  strata = 6L,
  dependence = NULL,
  seed = 1L
)
}
\arguments{
\item{data}{Training data (\code{sf}, matrix, or \code{data.frame}).}

\item{target}{A \code{\link[=prediction_target]{prediction_target()}} (the deployment region).}

\item{budget}{Number of validation points to place.}

\item{response}{Name of the response column (for the dependence/error model).}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{candidates}{Optional pool of candidate validation locations (default: the
target locations).}

\item{allocation}{\code{"optimal"} (Neyman, default), \code{"proportional"}, or \code{"random"}.}

\item{strata}{Number of distance-to-training strata.}

\item{dependence}{Optional \code{\link[=estimate_dependence]{estimate_dependence()}} object.}

\item{seed}{RNG seed.}
}
\value{
An object of class \code{validation_design}.
}
\description{
Given training locations and a deployment target, choose where to place a budget
of independent validation points so the resulting estimate of true map accuracy is
unbiased (its distance-to-training distribution matches deployment) and
minimum-variance (distance-stratified Neyman allocation using the GP error model).
}
\examples{
set.seed(1)
d <- data.frame(x = runif(80), y = runif(80), z = rnorm(80))
grid <- as.matrix(expand.grid(x = seq(0, 1, .1), y = seq(0, 1, .1)))
design_validation(d, prediction_target(grid = grid, type = "grid"),
                  budget = 20, response = "z", coords = c("x", "y"))
}
