% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st-leakage.R
\name{detect_st_leakage}
\alias{detect_st_leakage}
\title{Detect joint spatiotemporal leakage}
\usage{
detect_st_leakage(
  data,
  split,
  time,
  coords = NULL,
  sp_range = NULL,
  t_range = NULL,
  horizon = NULL
)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{split}{A split specification (see \code{\link[=detect_leakage]{detect_leakage()}}).}

\item{time}{Name of the time column (numeric or \code{Date}).}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{sp_range, t_range}{Spatial / temporal dependence ranges used to scale the two
axes (defaults: 30\% of the spatial extent / temporal span). Supply estimated
ranges for a calibrated metric.}

\item{horizon}{Forecast horizon for the default deployment target (defaults to the
median temporal spacing).}
}
\value{
An object of class \code{st_leakage}.
}
\description{
Measures leakage in a scaled space-time metric (each axis divided by its
dependence range) for a split, relative to a deployment target (by default a
one-step-ahead forecast). Reports the joint space-time index together with the
space-only and time-only indices, since the joint measure can differ from either.
}
