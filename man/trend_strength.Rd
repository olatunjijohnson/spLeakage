% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{trend_strength}
\alias{trend_strength}
\title{Trend strength: large-scale structure that resists extrapolation}
\usage{
trend_strength(data, response, coords = NULL, degree = 2L)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{response}{Name of the numeric response column.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{degree}{Polynomial degree of the coordinate trend surface (default 2).}
}
\value{
A single number in \verb{[0, 1]}: the trend \code{R^2}.
}
\description{
The proportion of response variance explained by a low-order polynomial of the
coordinates -- a measure of large-scale spatial trend. High trend strength means a
trend-blind model will extrapolate poorly, a leakage channel the autocorrelation
\code{\link[=detect_leakage]{detect_leakage()}} index does not capture.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(100), y = runif(100))
d$z <- 3 * d$x + rnorm(100, 0, 0.2)        # strong x-trend
trend_strength(d, "z", coords = c("x", "y"))
}
