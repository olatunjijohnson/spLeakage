% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emulator.R
\name{predict_optimism}
\alias{predict_optimism}
\title{Predict optimism from the calibrated emulator (fast, no model refitting)}
\usage{
predict_optimism(
  data,
  split,
  target,
  response = NULL,
  model = c("idw", "rf", "gam"),
  response_type = c("auto", "gaussian", "poisson", "binomial"),
  dependence = NULL,
  coords = NULL,
  emulator = NULL
)
}
\arguments{
\item{data}{An \code{sf} object, numeric coordinate matrix, or \code{data.frame}.}

\item{split}{A split specification: a \code{list(test=, train=)}, a list of
test-index vectors (k folds), or a fold-id vector of length \code{nrow(data)}.}

\item{target}{A \code{\link[=prediction_target]{prediction_target()}} (or coordinates coercible to one).}

\item{response}{Name of the numeric response column, used to estimate spatial
dependence (required unless \code{dependence} is supplied).}

\item{model}{The learner whose optimism to estimate: \code{"idw"} (default), \code{"rf"},
or \code{"gam"}.}

\item{response_type}{\code{"auto"} (detect from the response, default), \code{"gaussian"},
\code{"poisson"}, or \code{"binomial"}.}

\item{dependence}{A precomputed \code{\link[=estimate_dependence]{estimate_dependence()}} object (optional;
overrides \code{response}). Carries any anisotropy used.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{emulator}{An \code{optimism_emulator}; defaults to the one shipped with the package.}
}
\value{
An object of class \code{optimism_prediction}.
}
\description{
Estimates the optimism of a split using the pre-trained emulator and cheap
data/split/target features -- the fast counterpart to \code{\link[=estimate_optimism]{estimate_optimism()}}. The
estimate is conditioned on the model class and response type. If the query falls
outside the emulator's area of applicability (in feature or category space) the
function refuses a point estimate (\code{NA}) rather than extrapolating.
}
