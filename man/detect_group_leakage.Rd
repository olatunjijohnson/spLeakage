% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group-leakage.R
\name{detect_group_leakage}
\alias{detect_group_leakage}
\title{Detect grouped / duplicated-location leakage in a split}
\usage{
detect_group_leakage(data, split, group = NULL, coords = NULL, tol = 0)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{split}{A split specification (see \code{\link[=detect_leakage]{detect_leakage()}}).}

\item{group}{Optional name of a grouping column. If omitted, groups are derived
from the coordinates.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{tol}{When grouping by coordinates, the distance within which points are
treated as the same location (\code{0} = exact duplicates).}
}
\value{
An object of class \code{group_leakage}.
}
\description{
Flags test observations that share a group (e.g. the same site, repeated survey,
household, or plot) with a training observation in the same fold -- exact leakage
that the distance-based \code{\link[=detect_leakage]{detect_leakage()}} index registers only at distance zero.
}
\examples{
# Two sites, each measured twice; a random split co-locates train and test.
d <- data.frame(x = c(1, 1, 2, 2), y = c(1, 1, 2, 2), z = rnorm(4))
detect_group_leakage(d, split = c(1, 2, 1, 2), coords = c("x", "y"))
}
