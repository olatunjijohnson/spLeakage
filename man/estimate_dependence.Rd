% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependence.R
\name{estimate_dependence}
\alias{estimate_dependence}
\title{Estimate spatial dependence (variogram, range, correlation, anisotropy)}
\usage{
estimate_dependence(
  data,
  response,
  coords = NULL,
  n_bins = 15L,
  cutoff = NULL,
  anisotropy = NULL
)
}
\arguments{
\item{data}{An \code{sf} object, numeric coordinate matrix, or \code{data.frame}.}

\item{response}{Name of the (numeric) response column used for the variogram.}

\item{coords}{For non-\code{sf} input, column names/indices of the coordinates.}

\item{n_bins}{Number of variogram bins.}

\item{cutoff}{Maximum lag distance; defaults to half the maximum pairwise distance.}

\item{anisotropy}{\code{NULL} (isotropic, default), \code{"auto"} (estimate geometric
anisotropy from directional variograms; projected data only), or a
\code{list(angle =, ratio =)} giving the major-axis angle (radians) and the
minor/major range ratio in \verb{(0, 1]}.}
}
\value{
An object of class \code{sp_dependence} with elements \code{rho} (a function),
\code{range}, \code{practical_range} (\code{phi}), \code{psill}, \code{nugget}, \code{signal_prop},
\code{anisotropy} (or \code{NULL}), \code{coef}, \code{vcov}, and \code{variogram}.
}
\description{
Fits an empirical semivariogram and an exponential model, returning the
correlation function \code{rho(h)}, the variogram range, the practical range \code{phi},
optional geometric anisotropy, and the parameter covariance used to propagate
uncertainty into the Spatial Leakage Index.
}
