% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank-schemes.R
\name{rank_cv_schemes}
\alias{rank_cv_schemes}
\title{Rank candidate cross-validation schemes by deployment match}
\usage{
rank_cv_schemes(
  data,
  target,
  response,
  coords = NULL,
  k = 10L,
  schemes = c("random", "block", "buffered"),
  dependence = NULL,
  seed = 1L
)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{target}{A \code{\link[=prediction_target]{prediction_target()}} (or coordinates coercible to one).}

\item{response}{Name of the numeric response column (for the dependence model).}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{k}{Number of folds for each candidate scheme.}

\item{schemes}{Candidate schemes: any of \code{"random"}, \code{"block"}, \code{"buffered"}.}

\item{dependence}{Optional precomputed \code{\link[=estimate_dependence]{estimate_dependence()}} object.}

\item{seed}{RNG seed for the random scheme.}
}
\value{
An object of class \code{scheme_ranking}.
}
\description{
Builds each candidate CV scheme, measures its Spatial Leakage Index against the
declared prediction target, and ranks them: the scheme whose \code{SLI_rho} is closest
to zero best imitates deployment (neither optimistic nor pessimistic).
}
\examples{
set.seed(1)
d <- data.frame(x = runif(150), y = runif(150), z = rnorm(150))
grid <- as.matrix(expand.grid(x = seq(0, 1, .2), y = seq(0, 1, .2)))
rank_cv_schemes(d, prediction_target(grid = grid, type = "grid"),
                response = "z", coords = c("x", "y"))
}
