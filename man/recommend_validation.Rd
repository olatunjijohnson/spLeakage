% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{recommend_validation}
\alias{recommend_validation}
\title{Recommend a validation strategy for a sampling design and prediction target}
\usage{
recommend_validation(
  data = NULL,
  estimand = c("prediction", "population"),
  design = c("unknown", "probability", "clustered", "convenience"),
  target = c("grid", "interpolation", "newdata"),
  coords = NULL
)
}
\arguments{
\item{data}{Optional \code{sf}/matrix/\code{data.frame}, used only for the clustering flag.}

\item{estimand}{\code{"prediction"} (conditional predictive skill at locations,
default) or \code{"population"} (population-mean map accuracy over a region).}

\item{design}{\code{"unknown"} (default), \code{"probability"}, \code{"clustered"}, or
\code{"convenience"}. This is an elicited fact about data collection.}

\item{target}{\code{"grid"} (wall-to-wall, default), \code{"interpolation"}, or \code{"newdata"}.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}
}
\value{
An object of class \code{validation_recommendation}.
}
\description{
Operationalises the design x estimand x target framework: it asks for the
estimand and the sampling design (which cannot be inferred from coordinates) and
returns a ranked, conditional recommendation -- including when spatial CV is
\emph{not} appropriate. Any supplied data is used only to compute a clustering risk
flag, never to infer the design.
}
\examples{
# A clustered sample to be mapped wall-to-wall: spatial CV is appropriate.
recommend_validation(estimand = "prediction", design = "clustered", target = "grid")
# A probability sample: random CV is correct, spatial CV is over-pessimistic.
recommend_validation(estimand = "prediction", design = "probability", target = "grid")
}
