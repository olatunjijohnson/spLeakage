% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group-leakage.R
\name{group_kfold}
\alias{group_kfold}
\title{Group-aware k-fold assignment (the fix for grouped leakage)}
\usage{
group_kfold(data, k = 10L, group = NULL, coords = NULL, tol = 0)
}
\arguments{
\item{data}{An \code{sf} object, numeric matrix, or \code{data.frame}.}

\item{k}{Number of folds.}

\item{group}{Optional name of a grouping column. If omitted, groups are derived
from the coordinates.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{tol}{When grouping by coordinates, the distance within which points are
treated as the same location (\code{0} = exact duplicates).}
}
\value{
An integer fold-id vector of length \code{nrow(data)}.
}
\description{
Assigns whole groups to folds so that members of a group (e.g. co-located
records) are never split across train and test. The remedy for the leakage that
\code{\link[=detect_group_leakage]{detect_group_leakage()}} flags.
}
