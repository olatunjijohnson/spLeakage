% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect-leakage.R
\name{detect_leakage}
\alias{detect_leakage}
\title{Detect and quantify spatial leakage in a train/test split}
\usage{
detect_leakage(
  data,
  split,
  target,
  response = NULL,
  dependence = NULL,
  coords = NULL,
  n_boot = 0L,
  k = 1L
)
}
\arguments{
\item{data}{An \code{sf} object, numeric coordinate matrix, or \code{data.frame}.}

\item{split}{A split specification: a \code{list(test=, train=)}, a list of
test-index vectors (k folds), or a fold-id vector of length \code{nrow(data)}.}

\item{target}{A \code{\link[=prediction_target]{prediction_target()}} (or coordinates coercible to one).}

\item{response}{Name of the numeric response column, used to estimate spatial
dependence (required unless \code{dependence} is supplied).}

\item{dependence}{A precomputed \code{\link[=estimate_dependence]{estimate_dependence()}} object (optional;
overrides \code{response}). Carries any anisotropy used.}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{n_boot}{Number of Monte-Carlo draws from the variogram-fit uncertainty
used to put a confidence interval on the SLI. \code{0} (default) skips it. The
split geometry is exact, so only \code{rho}/\code{phi} uncertainty is propagated.}

\item{k}{Number of nearest training neighbours for the dependence-form \code{SLI_rho}
(density-aware, noisy-OR retained correlation). \code{1} (default, recommended) is
the single-nearest-neighbour index, which is near-sufficient for predicting
optimism (cor ~ 0.98 with exact GP optimism). \code{k > 1} improves the magnitude
match but reduces the correlation (it over-counts correlated neighbours), so it
is offered as an option rather than the default.}
}
\value{
An object of class \code{leakage_diagnosis}.
}
\description{
Audits an existing split or cross-validation scheme: computes the Spatial
Leakage Index (signed) in both a model-free distance form (\code{SLI_d}) and a
covariance-aware dependence form (\code{SLI_rho}), with a per-point leakage
decomposition. Positive values indicate optimistic leakage (CV easier than
deployment); negative values indicate pessimism. See \code{docs/METHOD-SLI.md}.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(80), y = runif(80), z = rnorm(80))
folds <- sample(rep_len(1:5, 80))
grid <- as.matrix(expand.grid(x = seq(0, 1, 0.2), y = seq(0, 1, 0.2)))
tgt <- prediction_target(grid = grid, type = "grid")
detect_leakage(d, folds, tgt, response = "z", coords = c("x", "y"))
}
