% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deleak.R
\name{deleak_estimate}
\alias{deleak_estimate}
\title{De-leaked (bias-corrected) accuracy estimate}
\usage{
deleak_estimate(
  data,
  split,
  response,
  reported = NULL,
  target = NULL,
  predict_fun = NULL,
  metric = c("rmse", "mae", "brier", "logloss", "poisson"),
  control = c("block", "buffer"),
  coords = NULL,
  dependence = NULL,
  k_control = NULL,
  buffer = NULL,
  n_boot = 500L,
  level = 0.9
)
}
\arguments{
\item{data}{An \code{sf} object, numeric coordinate matrix, or \code{data.frame}.}

\item{split}{The user's split: a \code{list(test=, train=)}, a list of test-index
vectors, or a fold-id vector of length \code{nrow(data)}.}

\item{response}{Name of the numeric response column.}

\item{reported}{Optional reported metric value to correct (e.g. a published RMSE
from a model not available for refitting). The de-leaked value is
\code{reported * (controlled / user-CV)} error ratio.}

\item{target}{Optional \code{\link[=prediction_target]{prediction_target()}}. When supplied, the controlled
scheme is \strong{target-matched} (the candidate scheme whose \code{SLI_rho} against the
target is closest to zero) instead of a fixed spatial block -- recommended when
deployment is extrapolation, where a fixed block under-corrects.}

\item{predict_fun}{A function \verb{(train_df, test_df)} returning numeric predictions
for the test rows. Defaults to inverse-distance weighting on the coordinates.
Custom learners (e.g. wrapping \code{lm}/\code{ranger}) let optimism be model-conditional.}

\item{metric}{Scoring rule (lower = better): \code{"rmse"} (default) or \code{"mae"} for
continuous responses; \code{"brier"} or \code{"logloss"} for binary responses (predictions
in \verb{[0, 1]}); \code{"poisson"} deviance for counts. Use a proper rule for the response
type so optimism is meaningful (e.g. Brier/log-loss for disease prevalence).}

\item{control}{The leakage-controlled comparison scheme: \code{"block"} (spatial
k-means blocks, default) or \code{"buffer"} (buffer the user's folds by the
practical range).}

\item{coords}{For non-\code{sf} input, the coordinate column names/indices.}

\item{dependence}{Optional \code{\link[=estimate_dependence]{estimate_dependence()}} object (used to set the buffer
for \code{control = "buffer"}).}

\item{k_control}{Number of control folds (defaults to the number of user folds).}

\item{buffer}{Buffer distance for \code{control = "buffer"} (defaults to the practical
range from \code{dependence}).}

\item{n_boot}{Number of fold-bootstrap resamples for the interval.}

\item{level}{Confidence level for the interval.}
}
\value{
An object of class \code{deleak_estimate}.
}
\description{
Reports the accuracy a model \emph{would} achieve under a leakage-controlled,
design-matched validation -- the de-leaked estimate practitioners should report --
with a fold (cluster) bootstrap confidence interval. Optionally corrects a
user-supplied \code{reported} metric value (e.g. a published RMSE) using the estimated
optimism ratio, without refitting the original model.
}
\examples{
set.seed(1)
d <- data.frame(x = runif(150), y = runif(150), z = rnorm(150))
deleak_estimate(d, split = sample(rep_len(1:10, 150)), response = "z",
                coords = c("x", "y"), n_boot = 100)
}
