# Extract the Spatial Leakage Index

Extract the Spatial Leakage Index

## Usage

``` r
sli(x, type = c("rho", "d"))
```

## Arguments

- x:

  A `leakage_diagnosis` object.

- type:

  `"rho"` (dependence form, default) or `"d"` (distance form).

## Value

A single numeric SLI value.
